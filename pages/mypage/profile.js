import MyManuItem from "@/components/MyPage/MyManuItem";

// FIXME :프로필 페이지에서는 마이페이지 모달이 가려져서 보이지 않는다. 수정 필요

const profile = ({ userId, manuscriptId }) => {

  return (
    <div>
        
    </div>
  );
};

export default profile;
