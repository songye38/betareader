// src/models/useEpisode.js

// useEpisode 훅은 특정 에피소드에 대한 정보를 가져오는 역할을 합니다.
// 주로 에피소드의 유형, 내용, 생성 일시 등을 다룹니다.
export const useEpisodeModel = (episodeId) => {
    // 에피소드 상태, 로딩 상태, 에러 상태를 관리
    // 서버에서 episodeId를 기반으로 에피소드 정보를 가져옴
    // 예: { id, type, content, created_at, last_edited_at }
  };
  