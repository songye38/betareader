// src/models/useProfile.js

// useProfile 훅은 사용자의 프로필 정보를 가져오는 역할을 합니다.
// 주로 사용자 프로필 페이지에서 사용자명, 아바타 등 프로필 관련 데이터를 가져옵니다.
export const useProfileModel = (userId) => {
    // 프로필 상태, 로딩 상태, 에러 상태를 관리
    // 서버에서 userId를 기반으로 프로필 데이터를 가져옴
    // 예: { username, avatar_url, created_at }
  };
  