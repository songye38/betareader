// src/models/useUser.js

// useUser 훅은 사용자 정보를 가져오는 역할을 합니다.
// 사용자 ID를 입력받아, 해당 사용자의 정보를 API로부터 받아옵니다.
export const useUserModel = (userId) => {
    // 사용자 상태, 로딩 상태, 에러 상태를 관리
    // 서버에서 userId를 기반으로 사용자 정보를 가져옴
    // 예: { id, email, created_at, last_sign_in_at }
  };
  